# Copy to .env.local for local overrides, or set as a Vercel project env var
# at build time. Leave unset locally — vite.config.ts already proxies /api
# to http://localhost:4000 in dev.

VITE_API_URL=https://your-backend-host.onrender.com
